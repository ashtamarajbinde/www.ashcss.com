//type conversion
//Q.2 here's a code that asks the user for two numbers and shows their sum
// it works incorrectly . the output in the example below is 12 (for default prompt values
   // ).why ? fix it. the result should be 3.
    // let a=prompt("first number ? " , 1);
    // let b=prompt("second number ? ",2);
    // addition=parseInt("first number ? " , 1)+("second number ? ",2)
    // console.log(output = +addition)




// type conversion 
// Q.1 what are the results if these expression?
    //ex.1
a="ash"+1+0
console.log(a)

//ex.2
b="sam"-1+0
console.log(b)

//ex.3

console.log(true+false)